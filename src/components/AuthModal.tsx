import React, { useState } from 'react';
import { useApp } from '../context/AppContext';
import { AlertCircle, ArrowRight, CheckCircle2, Lock, Mail, Shield, User, X } from 'lucide-react';

export const AuthModal: React.FC = () => {
  const { isAuthModalOpen, setIsAuthModalOpen, authModalTab, setAuthModalTab, login, setIsAdminOpen } = useApp();
  const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [name,setName]=useState(''); const [otp,setOtp]=useState(''); const [challengeId,setChallengeId]=useState(''); const [mode,setMode]=useState<'auth'|'forgot'|'verify-register'>('auth'); const [errorMsg,setErrorMsg]=useState(''); const [successMsg,setSuccessMsg]=useState(''); const [busy,setBusy]=useState(false);
  if(!isAuthModalOpen)return null;
  const close=()=>{if(busy)return;setIsAuthModalOpen(false);setErrorMsg('');setSuccessMsg('');setChallengeId('');setOtp('');setMode('auth')};
  const setTab=(tab:'login'|'register'|'admin')=>{setAuthModalTab(tab);setErrorMsg('');setSuccessMsg('');setChallengeId('');setOtp('');setMode('auth')};
  const serverUserLogin=(result:any)=>{if(result?.user)login(result.user.email,result.user.role==='admin'?'admin':'customer',result.user.name,result.user.provider||'email')};
  const submit=async()=>{
    setErrorMsg('');setSuccessMsg('');
    if(challengeId){if(!/^\d{6}$/.test(otp)){setErrorMsg('Enter the 6-digit verification code.');return}setBusy(true);try{const endpoint=authModalTab==='admin'?'/api/admin/verify-otp':mode==='forgot'?'/api/auth/reset-password':mode==='verify-register'?'/api/auth/verify-email-otp':'/api/auth/verify-login-otp';const body=authModalTab==='admin'?{challengeId,code:otp}:mode==='forgot'?{challengeId,code:otp,password}: {challengeId,code:otp};const r=await fetch(endpoint,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Verification failed.');if(authModalTab==='admin'){localStorage.setItem('arvex_admin_token',j.token);serverUserLogin(j);setSuccessMsg('Administrator identity verified.');setTimeout(()=>{close();setIsAdminOpen(true)},450)}else if(mode==='forgot'){setSuccessMsg('Password reset successfully. You can now sign in.');setChallengeId('');setOtp('');setPassword('');setMode('auth');setAuthModalTab('login')}else{serverUserLogin(j);setSuccessMsg(mode==='verify-register'?'Email verified successfully.':'Signed in successfully.');setTimeout(close,500)}}catch(x){setErrorMsg(x instanceof Error?x.message:'Verification failed.')}finally{setBusy(false)}return}
    if(mode==='forgot'){if(!email){setErrorMsg('Enter your account email address.');return}setBusy(true);try{const r=await fetch('/api/auth/forgot-password',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:email.trim().toLowerCase()})});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Unable to send reset code.');setChallengeId(j.challengeId);setSuccessMsg('A 6-digit reset code was sent to your email.')}catch(x){setErrorMsg(x instanceof Error?x.message:'Unable to send reset code.')}finally{setBusy(false)}return}
    if(!email||!password||(authModalTab==='register'&&!name)){setErrorMsg('Please complete all required fields.');return}setBusy(true);try{const endpoint=authModalTab==='admin'?'/api/admin/login':authModalTab==='register'?'/api/auth/register':'/api/auth/login';const body=authModalTab==='register'?{name:name.trim(),email:email.trim().toLowerCase(),password}:{email:email.trim().toLowerCase(),password};const r=await fetch(endpoint,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||'Authentication failed.');if(authModalTab==='register'){if(j.verificationRequired&&j.challengeId){setChallengeId(j.challengeId);setMode('verify-register');setSuccessMsg(j.message||'A 6-digit verification code was sent to your email.')}else setSuccessMsg(j.message||'Account created.');setPassword('');return}if(authModalTab==='admin'&&j.requiresTwoFactor){setChallengeId(j.challengeId);setSuccessMsg('Password accepted. A verification code was sent to the administrator email.');return}if(j.requiresTwoFactor&&j.challengeId){setChallengeId(j.challengeId);setSuccessMsg(j.message||'A verification code was sent to your email.');return}serverUserLogin(j);setSuccessMsg('Signed in successfully.');setTimeout(close,450)}catch(x){setErrorMsg(x instanceof Error?x.message:'Unable to reach authentication service.')}finally{setBusy(false)}};
  const googleLogin=()=>window.location.assign('/api/auth/google/start');
  return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-[#020208]/85 p-4 backdrop-blur-xl"><div className="relative w-full max-w-md overflow-hidden rounded-[30px] border border-white/10 bg-[#0b0d16] shadow-2xl"><div className="absolute inset-x-0 top-0 h-1 bg-gradient-to-r from-purple-600 via-fuchsia-400 to-indigo-500"/><div className="relative p-6 sm:p-8"><button onClick={close} className="absolute right-5 top-5 rounded-xl border border-white/5 bg-white/5 p-2 text-slate-400"><X className="h-4 w-4"/></button><div className="mb-6 flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center overflow-hidden rounded-2xl border border-purple-400/20"><img src="https://www.image2url.com/r2/default/images/1787805975676-5a4d373d-c6bd-4d39-bb64-1336474f4a7a.png" alt="ArveX Hosting" className="h-full w-full object-contain p-1.5"/></div><div><h3 className="font-display text-xl font-black text-white">{authModalTab==='admin'?'Admin Portal':'ArveX Account'}</h3><p className="text-xs text-slate-500">Secure authentication for ArveX Hosting</p></div></div>
  {!challengeId&&mode==='auth'&&<div className="mb-5 grid grid-cols-2 rounded-xl border border-white/5 bg-black/20 p-1 text-xs font-bold"><button type="button" onClick={()=>setTab('login')} className={`rounded-lg py-2.5 ${authModalTab==='login'?'bg-white text-black':'text-slate-500'}`}>Sign In</button><button type="button" onClick={()=>setTab('register')} className={`rounded-lg py-2.5 ${authModalTab==='register'?'bg-white text-black':'text-slate-500'}`}>Create Account</button></div>}
  {errorMsg&&<div className="mb-4 flex items-start gap-2 rounded-xl border border-rose-500/20 bg-rose-500/10 p-3 text-xs text-rose-300"><AlertCircle className="h-4 w-4"/><span>{errorMsg}</span></div>}{successMsg&&<div className="mb-4 flex items-start gap-2 rounded-xl border border-emerald-500/20 bg-emerald-500/10 p-3 text-xs text-emerald-300"><CheckCircle2 className="h-4 w-4"/><span>{successMsg}</span></div>}
  {!challengeId&&mode==='auth'&&authModalTab!=='admin'&&<><button type="button" onClick={googleLogin} className="flex w-full items-center justify-center gap-3 rounded-xl bg-white py-3 text-sm font-bold text-slate-900"><b>G</b>Continue with Google</button><div className="my-5 text-center text-[10px] text-slate-600">OR</div></>}
  <form onSubmit={e=>{e.preventDefault();submit()}} className="space-y-4">{challengeId?<><label className="block"><span className="mb-1.5 block text-xs font-semibold text-slate-300">6-digit verification code</span><input inputMode="numeric" autoComplete="one-time-code" maxLength={6} value={otp} onChange={e=>setOtp(e.target.value.replace(/\D/g,'').slice(0,6))} className="w-full rounded-xl border border-purple-400/20 bg-black/20 py-3 px-4 text-center text-lg font-black tracking-[.45em] text-white outline-none" placeholder="000000"/></label>{mode==='forgot'&&<label className="block"><span className="mb-1.5 block text-xs font-semibold text-slate-300">New Password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/20 py-3 px-4 text-sm text-white outline-none" placeholder="At least 10 characters"/></label>}</>:mode==='forgot'?<label className="block"><span className="mb-1.5 block text-xs font-semibold text-slate-300">Account Email</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/20 py-3 px-4 text-sm text-white outline-none" placeholder="you@example.com"/></label>:<>{authModalTab==='register'&&<label className="block"><span className="mb-1.5 block text-xs font-semibold text-slate-300">Full Name</span><input value={name} onChange={e=>setName(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/20 py-3 px-4 text-sm text-white outline-none" placeholder="Your name"/></label>}<label className="block"><span className="mb-1.5 block text-xs font-semibold text-slate-300">Email Address</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/20 py-3 px-4 text-sm text-white outline-none" placeholder="you@example.com"/></label><label className="block"><span className="mb-1.5 block text-xs font-semibold text-slate-300">Password</span><input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black/20 py-3 px-4 text-sm text-white outline-none" placeholder={authModalTab==='register'?'At least 10 characters':'Your password'}/></label>{authModalTab==='login'&&<button type="button" onClick={()=>{setMode('forgot');setErrorMsg('')}} className="text-xs font-semibold text-purple-300">Forgot password?</button>}</>}</form><button disabled={busy} type="button" onClick={submit} className="mt-4 flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-purple-600 to-indigo-600 py-3.5 text-sm font-bold text-white disabled:opacity-60">{busy?'Securing…':challengeId?(mode==='forgot'?'Reset Password':authModalTab==='admin'?'Verify & Enter Admin':mode==='verify-register'?'Verify Email':'Verify & Sign In'):mode==='forgot'?'Send Reset Code':authModalTab==='register'?'Create Secure Account':authModalTab==='admin'?'Continue to Verification':'Sign In'}{!busy&&<ArrowRight className="h-4 w-4"/>}</button>{mode==='forgot'&&!challengeId&&<button type="button" onClick={()=>{setMode('auth');setAuthModalTab('login')}} className="mx-auto mt-4 block text-xs text-slate-500">Back to sign in</button>}{authModalTab==='admin'&&!challengeId&&<p className="mt-5 flex items-center justify-center gap-1.5 text-center text-[10px] text-slate-600"><Shield className="h-3 w-3"/>Administrator login requires password + email verification.</p>}{authModalTab!=='admin'&&!challengeId&&mode==='auth'&&<button type="button" onClick={()=>setTab('admin')} className="mx-auto mt-3 block text-[10px] font-semibold text-slate-600">Staff / administrator sign-in</button>}</div></div>;
};
